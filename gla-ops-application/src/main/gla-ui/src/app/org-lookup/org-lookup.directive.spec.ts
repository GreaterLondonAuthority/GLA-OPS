import { OrgLookupDirective } from './org-lookup.directive';

describe('OrgLookupDirective', () => {
  it('should create an instance', () => {
    const directive = new OrgLookupDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
